<%= form_for(@book_genre) do |f| %>
  <% if @book_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_genre.errors.count, "error") %> prohibited this book_genre from being saved:</h2>

      <ul>
      <% @book_genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Book_id %><br>
    <%= f.text_field :Book_id %>
  </div>
  <div class="field">
    <%= f.label :Genre_id %><br>
    <%= f.text_field :Genre_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
